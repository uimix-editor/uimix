{
  "components": {
    // prebuilt UIMix assets (exposes ForeignComponents and tokens)
    "prebuilt": ["@uimix/mui-assets"],
    // paths to React components (component info is gathered by react-docgen-typescript)
    "react": ["./src/stories/*.tsx", "!./src/stories/*.stories.tsx"]
  },
  // paths to design token JSONs (W3C design tokens format)
  "designTokens": "./src/designTokens/*.json"
}
